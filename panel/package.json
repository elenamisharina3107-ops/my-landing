{
  "name": "admin-panel-server",
  "version": "1.0.0",
  "description": "Наш вход по email/паролю (маскирует GitHub) + история, экспорт, пользователи — деплоится на Beget в /admin/_panel/",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node serve.js",
    "test": "vitest run"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie": "^1.1.1"
  },
  "devDependencies": {
    "vitest": "^3.2.7"
  }
}
